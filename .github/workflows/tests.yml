name: Tests

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm test

  windows-powershell-compatibility:
    runs-on: windows-latest
    timeout-minutes: 5
    steps:
      # The repository contains a legacy filename ending in a dot, which NTFS
      # cannot materialize. Check out only this local helper for the Windows
      # compatibility job; the complete tree is exercised by the Linux job.
      - uses: actions/checkout@v4
        with:
          sparse-checkout: tools/Generate-JamesPreviewCredentials.ps1
          sparse-checkout-cone-mode: false
      - name: Verify credential helper with Windows PowerShell 5.1
        shell: powershell
        run: |
          if ($PSVersionTable.PSVersion.Major -ne 5) {
            throw "Expected Windows PowerShell 5.1."
          }
          .\tools\Generate-JamesPreviewCredentials.ps1 -SelfTest
